---
import { db, Screenshot, desc } from "astro:db";

import Layout from "../layouts/Layout.astro";

const towels = await db.select().from(Screenshot).orderBy(desc(Screenshot.id));

---

<Layout title="Xtowel" nav="full">
  <main>
    <header>
      <h1>Twowels, TwitterTowels, XTowe, Towelie?!?!</h1>
      <p>A towel is the most massively useful thing <s>an interstellar hitchhiker</s> a web developer can have!</p>
      <form method="post" action="/new" id="new"></form>
      <menu>
        <button form="new">Create an Xtowel</button>
      </menu>
    </header>
    <section>
      <h2>Featured Towels</h2>
      <div role="list">
        {
          towels.map((towel) => (
            <img src={towel.renderUrl} />
          ))
        }
      </div>
    </section>
  </main>
</Layout>

<style></style>
